% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/invoke_system.R
\name{invoke_system}
\alias{invoke_system}
\title{Invoke R's `system` for local commands}
\usage{
invoke_system(
  input,
  input_type = c("script", "command"),
  where = c("local", "hpc"),
  push_command = "sh",
  scheduler_arguments = NULL,
  fail_on_error = FALSE,
  echo = FALSE
)
}
\arguments{
\item{input}{A character string. Either the path to a script to execute or
a single line command.}

\item{input_type}{A character string. Either "script" or "command" to
indicate whether the input is a script path or a one-liner command.}

\item{where}{A character string. Either "local" or "hpc" to indicate whether
to submit the job to a HPC scheduler or run locally.}

\item{push_command}{A character string. The command to call to push the
script/command}

\item{scheduler_arguments}{A character vector of scheduler arguments.}

\item{fail_on_error}{If \code{TRUE}, fails when an error occurs. Otherwise,
just a warning is thrown.}

\item{echo}{Logical. If \code{TRUE}, prints final command to console.}
}
\value{
A character string of the job ID (for HPC submission) or process ID
(for local submission). If submission fails, returns \code{NULL}.
}
\description{
This function is a wrapper for R's native `system`/`system2`
functions that throws error codes in a standardized format and returns the
job ID/PID. It supports both HPC and local submission via the `where`
argument.
}
\details{
The function writes temporary stdout/stderr files to determine
job IDs and error messages.
}
\examples{
\dontshow{if (interactive() && nzchar(Sys.which("sbatch"))) withAutoprint(\{ # examplesIf}
invoke_system(input = "my_script.R", input_type = "script", where = "hpc")
\dontshow{\}) # examplesIf}
}
